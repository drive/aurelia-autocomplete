'use strict';

System.register([], function (_export, _context) {
  "use strict";

  var autoCompleteOptions;
  return {
    setters: [],
    execute: function () {
      _export('autoCompleteOptions', autoCompleteOptions = {
        suggestionTemplate: '<template>${selectedText}</template>'
      });

      _export('autoCompleteOptions', autoCompleteOptions);
    }
  };
});